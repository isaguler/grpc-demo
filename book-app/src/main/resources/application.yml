server:
  port: 8081

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:bookdb
    username: sa
    password: pass123
  jpa:
    hibernate:
      ddl-auto: update

grpc:
  client:
    warehouse:
      address: localhost:8083
      negotiation-type: plaintext
